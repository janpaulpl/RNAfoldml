let load_dot_string rna dots =
  if dots = (rna : Rna.t).seq then raise (Failure "not implemented")
  else failwith "nope still not"

let load_dot f =
  if f = "" then raise (Failure "not implemented")
  else failwith "nope still not"

let load_ct f =
  if f = "" then raise (Failure "not implemented")
  else failwith "nope still not"
